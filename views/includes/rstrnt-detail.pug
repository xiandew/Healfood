.detail
    .name #{r.name}
    .photo
        img(src=`${r.photo}`, alt=`${r.name}`)
    .overview
        .title Overview
        | #{r.description}
    .info
        .title Address
        a.address(href="/maps")
            i.fas.fa-map-marker-alt
            | #{r.address}

